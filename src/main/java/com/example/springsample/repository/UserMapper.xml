<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springsample.repository.UserMapper">
    <select id="search" resultType="com.example.springsample.entity.User">
        SELECT * FROM User WHERE id = #{id}
    </select>
</mapper>